using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ulei : MonoBehaviour
{
    public int Sota;

    // Start is called before the first frame update
    void Start()
    {
        Sota = 0;
    }

    
    // Update is called once per frame
    void Update()
    {
        
    }
}
